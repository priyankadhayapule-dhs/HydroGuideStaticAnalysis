<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background rectangle with border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/av_darkest_blue" />
            <stroke android:color="@color/av_light_blue" android:width="2dp" />
            <corners android:radius="4dp" />
            <padding android:left="8dp" android:right="8dp" android:top="6dp" android:bottom="6dp" />
        </shape>
    </item>
    <!-- Dropdown arrow on the right -->
    <item android:right="8dp" android:gravity="right|center_vertical" android:width="20dp" android:height="20dp">
        <shape android:shape="rectangle">
            <size android:width="20dp" android:height="20dp" />
        </shape>
    </item>
</layer-list>
